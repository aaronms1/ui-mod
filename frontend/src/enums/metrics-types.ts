/**
 * <h1>{@link MetricsTypes}</h1>
 * Enum representing the different types of metrics that can be monitored.
 */
export enum MetricsTypes {
  CPU = 'CPU',
  GPU = 'GPU',
  DISK = 'DISK',
  MEMORY = 'MEMORY',
  NETWORK = 'NETWORK',
  SERVER = 'SERVER'
}