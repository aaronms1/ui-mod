/**
 * <h1>{@link DownloadActions}</h1>
 * Enum is used to define the different actions that can be performed when downloading files.
 */
export enum DownloadActions {
  API_TOKEN = 'API_TOKEN',
  DOWNLOAD_LLM_JSON = 'DOWNLOAD_LLM_JSON',
  DOWNLOAD_LLM_MODEL = 'DOWNLOAD_LLM_MODEL',
  SEARCH = 'SEARCH'
}