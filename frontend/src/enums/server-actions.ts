/**
 * <h1>{@link ServerActions}</h1>
 * Enum representing the different actions that can be performed on a server.
 */
export enum ServerActions {
  START = 'START',
  STOP = 'STOP',
  RESTART = 'RESTART',
  PING = 'PING'
}