/**
 * <h1>{@link ModelActions}</h1>
 * Enum representing the different actions that can be performed on models.
 */
export enum ModelActions {
  CREATE = 'CREATE',
  CLONE = 'CLONE',
  DESTROY = 'DESTROY',
  MERGE = 'MERGE',
  SETTINGS = 'SETTINGS',
  TRAIN = 'TRAIN'
}